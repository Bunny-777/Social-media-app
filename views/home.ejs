<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="w-full flex justify-between items-center">
            <a class="bg-blue-500 rounded-md px-3 py-2 inline-block" href="/profile">Profile</a>
            <a class="bg-red-500 rounded-md px-3 py-2 inline-block" href="/logout">Logout</a>
        </div>
        <h3 class="text-3xl mt-5">Hello, <%= user.name %> 👋</h3>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">All Posts.</h3>
        </div>
        
        <div class="postcontainer mt-5"> 
            <% allPosts.reverse().forEach(function(post){ %>
                <div class="post w-1/3 p-5 mb-2 border-[1px] rounded-md bg-zinc-800 border-zinc-700">
                    <h4 class="text-pink-500">@ <%= post.user ? post.user.username : "Unknown" %> </h4>
                    <p class=""> <%= post.content %>  </p>
                    <div class="btns flex mt-2 gap-10">
                        <a class="text-blue-500" href="">Like</a>
                    </div>
                </div>
            <% }) %>
        </div>   
    </div>
</body>
</html>
